loader 加载于0x7c00 大小512

如果elf文件的载位置和链接的相对地址一致，则不需要重定位，如果进行了重定位，则可以加载到任意位置

1. 检测内存，ards数目保存于0x7b00，ards信息保存于0x7b04
2. 加载gdt, 代码段,数据段均0~4G,栈段0x7e00~0x8e00
3. 进入保护模式
4. 加载kernel到0x100000，内核大小不能超过128K(0x20000);
5. 内核重定位，入口地址为0x200000

